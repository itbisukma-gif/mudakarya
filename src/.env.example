# Supabase credentials
# These values can be found in your Supabase project settings under "API".
# Copy this file to .env.local for local development and fill in the values.
# For production (Vercel), add these as Environment Variables in the project settings.

NEXT_PUBLIC_SUPABASE_URL="https://your-project-url.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-public-anon-key"

# This is the service role key, used for server-side actions that need to bypass RLS.
# Found in your Supabase project settings under "API". Treat this as a high-privilege secret.
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"
